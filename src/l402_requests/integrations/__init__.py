"""Framework integrations for l402-requests.

Optional integrations with LangChain, CrewAI, and AutoGen.
Install the relevant extras to use:

    pip install l402-requests[langchain]
    pip install l402-requests[crewai]
    pip install l402-requests[autogen]
"""
